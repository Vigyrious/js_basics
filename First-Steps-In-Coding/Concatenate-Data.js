function concatenateData(first_name, last_name, num, town) {
    let fname = first_name;
    let lname = last_name;
    let age = Number(age);
    let city = town;
    console.log(`You are ${fname} ${lname}, a ${age}-years old person from ${city}`);
}