function ZooStore(arg1, arg2) {
    let my_dogs = Number(arg1);
    let other_animals = Number(arg2);
    dogs = my_dogs * 2.50;
    others = other_animals * 4;
    animal_sum = dogs + others;
    console.log(`${animal_sum} lv.`);
}